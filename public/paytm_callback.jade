doctype html
html
    head
        title Paytm
        script(type="javascript").
            function response() {return document.getElementById('response').value}
    body
        Redirecting back to app<br>
        form(method='post' name='frm')
            input(id='response', name='responseField', value=callback_data)